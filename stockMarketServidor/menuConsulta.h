#ifndef MENU_CONSULTA_H
#define MENU_CONSULTA_H

#include <winsock2.h>  // para que SOCKET esté definido

// Declaración de la función que muestra el menú consulta, recibe el socket de comunicación
void mostrarMenuConsulta(SOCKET comm_socket);

#endif // MENU_CONSULTA_H
